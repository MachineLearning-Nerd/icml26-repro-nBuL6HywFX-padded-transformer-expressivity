# C1–C2 — Volume and precision


---
<!-- trackio-cell
{"type": "markdown", "id": "cell_40d52fddb37f", "created_at": "2026-07-17T09:10:47+00:00", "title": "Definitions 2.2–2.3 define representation volume as V(N)=D(N)·b(N) and sufficie…"}
-->
Definitions 2.2–2.3 define representation volume as `V(N)=D(N)·b(N)` and sufficient volume as Ω(log N). The audit encodes every position for N through 4,096 and confirms the exact finite boundary `D·b≥ceil(log2 N)`. It separately records that log precision retains each tested threshold count while fixed b=3 eventually cannot.
