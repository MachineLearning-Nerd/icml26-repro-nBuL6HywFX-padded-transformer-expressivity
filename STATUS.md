# Publication status

The fresh official verdict at Space SHA
`c23407b0b2e93b0e1c07607a0a504e85af5d411e` is 0/4: both claims are
`inconclusive` because the obligation ledger and prose repair were not formal
or independently verified.

Attempt 3 is complete locally and uses a materially different evidence class:

- Lean 4.32 kernel formalization pinned by `formal/lean-toolchain`;
- 11 `#print axioms` reports and no `sorryAx` dependency;
- no `sorry`, `admit`, custom `axiom`, or `unsafe` token in executable Lean;
- universal exact layer-1 source routing and layer-2 gate collection;
- universal rejection of the printed layer-2 route when source differs from gate;
- exact constant six-coordinate repair, width padding, threshold arithmetic,
  inclusion composition, and induction over every circuit level;
- machine-readable source/toolchain certificate;
- 16/16 tests passing;
- public Trackio artifact `repro-bundle:v5` uploaded to
  `DineshAI/nBuL6HywFX-artifacts`.

Published to the existing judged Space at SHA
`f35da0ddf09afe55960035da73d2bb2a5264ecf2`. The content validator passes;
the exact legacy target reports only the known `repro-` slug incompatibility.
All 25 scoped public files match local SHA-256 hashes, the Space is running,
and the static logbook, poster, Lean source, and certificate return HTTP 200.
Next: await the fresh official verdict and use any remaining claim-specific
feedback for attempt 4. No GitHub commit or push has been made.
